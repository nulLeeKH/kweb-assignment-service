import DocController from './doc'
import AuthController from './auth'
import LmsController from './lms'

const controllers: any[] = [new DocController(), new AuthController(), new LmsController()]

export default controllers
